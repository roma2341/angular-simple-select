import { SelectorOptionLabelPipe } from './selector-option-label-pipe';

describe('SelectorOptionLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectorOptionLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
